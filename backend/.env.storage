# Storage Configuration
# Options: 'local' or 's3'
# - local: Stores files in backend/uploads folder
# - s3: Stores files in AWS S3 (requires AWS credentials)
STORAGE_TYPE=local

# Backend URL (for generating file URLs)
BACKEND_URL=http://localhost:5000

# AWS S3 Configuration (only needed if STORAGE_TYPE=s3)
# Leave empty if using local storage
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
AWS_FOLDER_NAME=wellness-images
